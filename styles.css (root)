:root{
  --bg:#0a0f1a; --card:#121a2a; --ink:#eaf1ff; --muted:#9fb3ff; --line:#1b2744; --accent:#0b5fff;
}
*{box-sizing:border-box;font-family:system-ui, -apple-system, Segoe UI, Roboto, Inter, sans-serif}
html,body{margin:0;height:100%;background:var(--bg);color:var(--ink)}
.wrap{max-width:1100px;margin:0 auto;padding:18px}
h1{margin:0 0 12px;font-size:22px}
.sub{margin:0 0 16px;color:var(--muted)}
.grid{display:grid;gap:12px;grid-template-columns:repeat(auto-fill,minmax(260px,1fr))}
.card{display:block;background:var(--card);border:1px solid var(--line);border-radius:14px;padding:16px;color:var(--ink);text-decoration:none}
.card:hover{outline:2px solid var(--accent)}
section.tool{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:16px;margin:12px 0}
label{display:block;font-size:13px;margin:8px 0 4px;color:var(--muted)}
input,select,button,textarea{
  width:100%;padding:12px;border-radius:10px;border:1px solid var(--line);background:#0e1626;color:var(--ink);
}
.row{display:grid;gap:10px;grid-template-columns:1fr 1fr}
button{background:#1460ff;border-color:#1a52d6;cursor:pointer}
button:active{transform:translateY(1px)}
.result{margin-top:8px;padding:10px;background:#0e1626;border:1px dashed var(--line);border-radius:10px;white-space:pre-wrap}
hr{border:0;border-top:1px solid var(--line);margin:10px 0}
small, .note{color:var(--muted)}
